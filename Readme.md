# MiniSub Blockly Frontend

Vite-based Blockly app for Milestone 1.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`

Command blocks POST JSON payloads to `http://192.168.4.1/api/v1/command`.
